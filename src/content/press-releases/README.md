# Press release content

This directory is reserved for structured Project South press release content.

The global navigation links to `/press-releases`; individual releases must not be added to the header dropdown.

The archive implementation will support structured fields such as title, date, slug, excerpt, body, featured image, category, and external links.
