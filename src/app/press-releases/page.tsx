import { FoundationPlaceholder } from "@/components/ui/FoundationPlaceholder";

export default function PressReleasesPage() {
  return (
    <FoundationPlaceholder
      label="Press Releases"
      title="Press Releases"
      note="This route will become the searchable, filterable press release archive. Individual releases will not appear in the global navigation."
    />
  );
}
